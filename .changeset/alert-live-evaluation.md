---
"@jpowersdev/signoz": minor
---

Add `signoz alerts evaluate` to replay supported v2 metric, log, and trace alert queries against live telemetry and summarize grouped values alongside configured thresholds.
